






add searching commits for changed files
git log --oneline --name-only --



git log --oneline --name-only -- path/one path/two dir/three/

follow rename single file only
git log --follow --oneline --name-only -- path/to/file



we need domysloot in "$HOME/.local/bin"
we need config in "$REPO_DIR/.config"
we need excludes in "$HOME/.config/sloot"

expanded="$(envsubst <<<"$text")"


if not .config curl -fsSL "$GH_URL/<file name and/or path>"

